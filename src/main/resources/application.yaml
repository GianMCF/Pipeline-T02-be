server:
  port: ${PORT:8088}
  address: 0.0.0.0

spring:
  application:
    name: vinumaw-backend

  data:
    mongodb:
      uri: ${MONGO_URI} # mongodb+srv://alejandrosotocardenas_db_user:F1Qvd3Dg5ueBpAvF@cluster0.y3mucnr.mongodb.net/vinum_aw?retryWrites=true&w=majority |||||| mongodb+srv://Vaw:VINUM2607@clusterz.q7d9mxv.mongodb.net/?appName=ClusterZ
      database: ${DB}

# JWT

jwt:
  secret: ${JWT_SECRET} # mySuperSecretKeyForJwtAuthentication2026Secure
  expiration: 86400000

# AES ENCRYPTION

aes:
  secret: ${AES_SECRET} # vinumawsecurekey

# SWAGGER

springdoc:
  api-docs:
    enabled: true
    path: /api-docs

  swagger-ui:
    enabled: true
    path: /swagger-ui.html

# CORS ACCESS

cors:
  allowed-origin: ${CORS_ALLOWED_ORIGIN}

# LOGGING

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG